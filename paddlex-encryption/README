给到的库分两部分：

1. 加密模型和参数文件部分
    1.1 提供了工具直接进行加密，涉及到key的地方可以使用 -g 参数生成，
        如果需要自行生成 key 需先生成32字节数据并经过base64编码后传入否则sdk无法通过检测（这里考虑到了 key 的生成会包含可见字符和不可见字符，不易破解 key 的数据）
    1.2 libpmodel-encrypt.so 的 API，可根据头文件和动态库自行集成
    
2. 解密模型和参数部分
    头文件和libpmodel-decrypt.so

注：
   1. 上述库的编译环境为标准 centos 7 下的产出
   2. 加密 tool 工具的基本使用 -h 参数查看
   3. 涉及到的paddle相关的头文件位置视实际使用时进行修改